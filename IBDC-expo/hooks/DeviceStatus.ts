import React from 'react';

// Should trigger when a bluetooth connection has been made
// Parameter is optional now as we don't have an object to parse values from the IBDC sensor yet
export function DeviceStatus(message?: object) {

}

